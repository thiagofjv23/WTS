# Assets

Coloque aqui a imagem de plano de fundo do jogo:

- **`taekwondo-bg.jpg`** — usada como fundo de todas as telas
  (referenciada em `src/styles/main.css`, regra `body::before`).

Basta salvar a imagem exatamente com esse nome neste diretório.
